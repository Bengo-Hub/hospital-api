// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/hospital-service/internal/ent/ward"
	"github.com/google/uuid"
)

// Ward is the model entity for the Ward schema.
type Ward struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// OutletID holds the value of the "outlet_id" field.
	OutletID uuid.UUID `json:"outlet_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Classification only — does not replace billable_item_code, which still prices this specific ward. Lets the UI group/filter wards and suggest (not force) a sensible default billable_item_code when a new ward is created
	WardType ward.WardType `json:"ward_type,omitempty"`
	// Informational headcount target; a bed-occupancy view counts real Bed rows, not this
	Capacity int `json:"capacity,omitempty"`
	// BillableItemCatalog code (department=inpatient) pricing a day in THIS ward, e.g. BED_DAY_ICU vs BED_DAY_GENERAL. Nil falls back to the tenant's default WARD_DAY_RATE code — see inpatient.Service's discharge-time ward-charge computation.
	BillableItemCode *string `json:"billable_item_code,omitempty"`
	// IsActive holds the value of the "is_active" field.
	IsActive bool `json:"is_active,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WardQuery when eager-loading is set.
	Edges        WardEdges `json:"edges"`
	selectValues sql.SelectValues
}

// WardEdges holds the relations/edges for other nodes in the graph.
type WardEdges struct {
	// Beds holds the value of the beds edge.
	Beds []*Bed `json:"beds,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BedsOrErr returns the Beds value or an error if the edge
// was not loaded in eager-loading.
func (e WardEdges) BedsOrErr() ([]*Bed, error) {
	if e.loadedTypes[0] {
		return e.Beds, nil
	}
	return nil, &NotLoadedError{edge: "beds"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Ward) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ward.FieldIsActive:
			values[i] = new(sql.NullBool)
		case ward.FieldCapacity:
			values[i] = new(sql.NullInt64)
		case ward.FieldName, ward.FieldWardType, ward.FieldBillableItemCode:
			values[i] = new(sql.NullString)
		case ward.FieldCreatedAt, ward.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case ward.FieldID, ward.FieldTenantID, ward.FieldOutletID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Ward fields.
func (_m *Ward) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ward.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case ward.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				_m.TenantID = *value
			}
		case ward.FieldOutletID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field outlet_id", values[i])
			} else if value != nil {
				_m.OutletID = *value
			}
		case ward.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case ward.FieldWardType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ward_type", values[i])
			} else if value.Valid {
				_m.WardType = ward.WardType(value.String)
			}
		case ward.FieldCapacity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field capacity", values[i])
			} else if value.Valid {
				_m.Capacity = int(value.Int64)
			}
		case ward.FieldBillableItemCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field billable_item_code", values[i])
			} else if value.Valid {
				_m.BillableItemCode = new(string)
				*_m.BillableItemCode = value.String
			}
		case ward.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				_m.IsActive = value.Bool
			}
		case ward.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case ward.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Ward.
// This includes values selected through modifiers, order, etc.
func (_m *Ward) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBeds queries the "beds" edge of the Ward entity.
func (_m *Ward) QueryBeds() *BedQuery {
	return NewWardClient(_m.config).QueryBeds(_m)
}

// Update returns a builder for updating this Ward.
// Note that you need to call Ward.Unwrap() before calling this method if this Ward
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Ward) Update() *WardUpdateOne {
	return NewWardClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Ward entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Ward) Unwrap() *Ward {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Ward is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Ward) String() string {
	var builder strings.Builder
	builder.WriteString("Ward(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("outlet_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.OutletID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("ward_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.WardType))
	builder.WriteString(", ")
	builder.WriteString("capacity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Capacity))
	builder.WriteString(", ")
	if v := _m.BillableItemCode; v != nil {
		builder.WriteString("billable_item_code=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsActive))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Wards is a parsable slice of Ward.
type Wards []*Ward
