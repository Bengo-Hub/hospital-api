// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/hospital-service/internal/ent/hospitaluseroutlet"
	"github.com/google/uuid"
)

// HospitalUserOutlet is the model entity for the HospitalUserOutlet schema.
type HospitalUserOutlet struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// Local HospitalUser.ID
	UserID uuid.UUID `json:"user_id,omitempty"`
	// Outlet id owned by auth-service
	OutletID uuid.UUID `json:"outlet_id,omitempty"`
	// True for the user's primary outlet
	IsHomeOutlet bool `json:"is_home_outlet,omitempty"`
	// auth-service user id of the admin who made the assignment, or nil for an auto-reconciled row
	AssignedBy *uuid.UUID `json:"assigned_by,omitempty"`
	// AssignedAt holds the value of the "assigned_at" field.
	AssignedAt   time.Time `json:"assigned_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HospitalUserOutlet) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case hospitaluseroutlet.FieldAssignedBy:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case hospitaluseroutlet.FieldIsHomeOutlet:
			values[i] = new(sql.NullBool)
		case hospitaluseroutlet.FieldAssignedAt:
			values[i] = new(sql.NullTime)
		case hospitaluseroutlet.FieldID, hospitaluseroutlet.FieldTenantID, hospitaluseroutlet.FieldUserID, hospitaluseroutlet.FieldOutletID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HospitalUserOutlet fields.
func (_m *HospitalUserOutlet) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case hospitaluseroutlet.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case hospitaluseroutlet.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				_m.TenantID = *value
			}
		case hospitaluseroutlet.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				_m.UserID = *value
			}
		case hospitaluseroutlet.FieldOutletID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field outlet_id", values[i])
			} else if value != nil {
				_m.OutletID = *value
			}
		case hospitaluseroutlet.FieldIsHomeOutlet:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_home_outlet", values[i])
			} else if value.Valid {
				_m.IsHomeOutlet = value.Bool
			}
		case hospitaluseroutlet.FieldAssignedBy:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field assigned_by", values[i])
			} else if value.Valid {
				_m.AssignedBy = new(uuid.UUID)
				*_m.AssignedBy = *value.S.(*uuid.UUID)
			}
		case hospitaluseroutlet.FieldAssignedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field assigned_at", values[i])
			} else if value.Valid {
				_m.AssignedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the HospitalUserOutlet.
// This includes values selected through modifiers, order, etc.
func (_m *HospitalUserOutlet) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this HospitalUserOutlet.
// Note that you need to call HospitalUserOutlet.Unwrap() before calling this method if this HospitalUserOutlet
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *HospitalUserOutlet) Update() *HospitalUserOutletUpdateOne {
	return NewHospitalUserOutletClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the HospitalUserOutlet entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *HospitalUserOutlet) Unwrap() *HospitalUserOutlet {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: HospitalUserOutlet is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *HospitalUserOutlet) String() string {
	var builder strings.Builder
	builder.WriteString("HospitalUserOutlet(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("outlet_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.OutletID))
	builder.WriteString(", ")
	builder.WriteString("is_home_outlet=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsHomeOutlet))
	builder.WriteString(", ")
	if v := _m.AssignedBy; v != nil {
		builder.WriteString("assigned_by=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("assigned_at=")
	builder.WriteString(_m.AssignedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// HospitalUserOutlets is a parsable slice of HospitalUserOutlet.
type HospitalUserOutlets []*HospitalUserOutlet
